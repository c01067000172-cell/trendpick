services:
  - type: web
    name: trendpick
    runtime: python
    plan: 0.5c-512mb
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    healthCheckPath: /
    envVars:
      - key: NAVER_CLIENT_ID
        sync: false
      - key: NAVER_CLIENT_SECRET
        sync: false
      - key: TRENDPICK_DATA_DIR
        value: /var/data/trendpick
      - key: COLLECTOR_INTERVAL_SECONDS
        value: "3600"
    disk:
      name: trendpick-data
      mountPath: /var/data/trendpick
      sizeGB: 1
